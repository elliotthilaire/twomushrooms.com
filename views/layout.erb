<!DOCTYPE html>
<html>
  <head>
    <title>Two Mushrooms (Photography Project by Elliott Hilaire)</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="I'm a software developer from Brisbane. When I'm outside I will either be on a bike or pestering insects for their photo.">
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">
    <!-- load css from sinatra-assetpack -->
    <%= css :app, :media => 'screen' %>

    <% if @opengraph %>
      <meta property="og:site_name" content="Two Mushrooms"/>
      <meta property="og:url" content="<%= @opengraph[:url] %>" />
      <meta property="og:description" content="<%= @opengraph[:description] %>"/>
      <meta property="og:title" content="<%= @opengraph[:title] %>" />
      <meta property="og:image" content="<%= @opengraph[:image] %>" />
    <% end %>

  </head>

  <body>

    <!-- load js from sinatra-assetpack -->
    <!-- this needs to stay near the top of the file until js is moved out of html -->
    <%= js :app %>

    <div id="top"></div>
    <div class="container">
      <!-- nagivation menu -->
      <% if !request.xhr? %>
        <div class="row">
          <nav class="navbar navbar-default" role="navigation">
            <div class="navbar-header">
              <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
              </button>
              <a class="navbar-brand" href="http://elliotthilaire.net">Elliott Hilaire</a>
            </div>
            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
              <ul class="nav navbar-nav navbar-right">
                <li class="<%= "active" if request.path_info == '/' %>"><a href="/">Home</a></li>
                <li class="<%= "active" if request.path_info == '/gallery' %>"><a href="/gallery">Gallery</a></li>
                <li class="<%= "active" if request.path_info == '/about' %>"><a href="/about">About</a></li>
              </ul>
            </div><!-- /.navbar-collapse -->
          </nav>
        </div><!--.row-->
      <%end%>

      <%= yield %>

      <footer></footer>
    </div><!--.container-->

    <%if settings.environment  == :production%>
    <script>
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

      ga('create', 'UA-39711596-1', 'twomushrooms.com');
      ga('send', 'pageview');
    </script>
    <%end%>

  </body>
</html>
